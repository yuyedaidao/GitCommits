# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def common
  pod 'Alamofire'
  pod 'GRDB.swift'
  pod 'YQLogger'
  pod 'SwiftDate'
end

target 'GitCommits (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  common
  
end

target 'GitCommits (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  common

end
